{
  "name": "@shogitter/ai",
  "version": "0.0.5",
  "description": "AI for shogi variants",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    "types": "./lib/index.d.ts",
    "default": "./lib/index.js"
  },
  "scripts": {
    "clean": "rm -rf lib",
    "build": "npm run clean && tsc && npm run build:copyjs",
    "build:copyjs": "cd src && find . -name '*.js' | cpio -pdm ../lib && cd -",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "pnpm build",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "na2hiro",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "@shogitter/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
