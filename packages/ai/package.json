{
  "name": "@shogitter/ai",
  "version": "0.0.2",
  "description": "",
  "type": "module",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./lib/index.d.ts",
        "default": "./lib/index.js"
      }
    }
  },
  "scripts": {
    "clean": "rm -rf lib",
    "build:bun": "bun build src/botRegistry.ts --outdir=lib",
    "build": "npm run clean && tsc && npm run build:copyjs",
    "build:copyjs": "cd src && find . -name '*.js' | cpio -pdm ../lib && cd -",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "pnpm build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@shogitter/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
